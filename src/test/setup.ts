// Runs before every test file (configured via vite.config.ts `test.setupFiles`).
// Adds jest-dom matchers (toBeInTheDocument, toHaveTextContent, ...) to `expect`.
import '@testing-library/jest-dom';
